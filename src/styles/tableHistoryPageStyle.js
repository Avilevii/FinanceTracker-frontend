export const styleBoxOption = {
    display: 'flex',
    justifyContent: 'center',
    gap: 4,
    my: 3,

    
};

export const styleInputDate = {
    display: 'flex',
    justifyContent: 'center',
    mt: 3,
    gap: 4,
}

export const styleDateRange = {
    textField: {
        InputProps: {
        sx: {
            borderRadius: 20,
            width: 200,
        }
        }
    }
}